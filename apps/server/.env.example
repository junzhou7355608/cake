# 服务端口（可选，默认 3000）
# PORT=3000

# 运行环境：development | production（可选，默认 development）
# NODE_ENV=development

# PostgreSQL 连接字符串（必填）
# 使用 docker-compose 启动后，运行 pnpm db:migrate 初始化数据库
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cake"
